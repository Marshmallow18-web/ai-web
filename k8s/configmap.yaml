apiVersion: v1
kind: ConfigMap
metadata:
  name: devsight-config
  namespace: devsight-ai
data:
  PORT: "4000"
  NODE_ENV: "production"
  DATABASE_URL: "postgresql://postgres:postgrespassword@postgres-service.devsight-ai.svc.cluster.local:5432/devsight?schema=public"
  APP_URL: "https://devsight.yourdomain.com"
